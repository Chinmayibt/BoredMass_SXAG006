OPENALEX_EMAIL=
CROSSREF_MAILTO=
SOURCE_TIMEOUT_SECONDS=15
OPENROUTER_API_KEY=
GROQ_API_KEY=
CACHE_TTL_SECONDS=1800

# Set to 1 to skip loading the embedding model at API startup (avoids paging-file / OOM on low-RAM Windows).
# The model may still load on first pipeline use that needs embeddings.
SKIP_EMBEDDING_WARMUP=
